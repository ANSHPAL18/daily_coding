#include <stdio.h>
#include <math.h>

int main()
{int n;
    printf("enter a number : ");
    scanf("%d", &n);
    int root = sqrt(n);
    printf("the square root of number is : %d ", root);
    return 0;
}